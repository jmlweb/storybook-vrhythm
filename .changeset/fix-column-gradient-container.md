---
"storybook-vrhythm": patch
---

Fix column gradient height (was 1px, now 100%) and allow overlay to move when container changes between stories. Invalid container selectors now fall back to body instead of crashing.
